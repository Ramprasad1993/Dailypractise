Git commands

git add filename
git commit -m "commit comments"
git push origin -b branchname
git stauts
git checkout branchname
git branch //to know in which branch you are


//once you push the file to origin branch this will fall under pull request to respective leads.

To get any new project in to your space click on fork
To get code in your local pc use git clone "path of ssh" --//this is from git hub


When we get conflicts

git merge origin/master
